{
  "name": "root",
  "private": true,
  "scripts": {
    "serve": "npm run serve --workspace=docs",
    "build": "npm run build --workspaces",
    "build:package": "npm run build --workspace=package",
    "build:docs": "npm run build --workspace=package",
    "lint": "npm run lint --workspaces",
    "backstop:test": "npm run backstop:test --workspace=package",
    "backstop:clean": "npm run backstop:clean --workspace=package",
    "backstop:approve": "npm run backstop:approve --workspace=package",
    "prettier-format": "npm run prettier-format --workspaces"
  },
  "workspaces": [
    "package",
    "docs"
  ],
  "devDependencies": {
    "@babel/types": "^7.17.0",
    "@highlightjs/vue-plugin": "^2.1.0",
    "@types/node": "^20.4.8",
    "@vitejs/plugin-vue": "^4.2.3",
    "backstopjs": "^5.4.4",
    "highlight.js": "^11.5.1",
    "nhsuk-frontend": "^6.1.2",
    "prettier": "^3.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6",
    "vite": "^4.4.9",
    "vue": "^3.3.4",
    "vue-router": "^4.2.4",
    "vue-tsc": "^1.8.8"
  }
}
