import { NhsHeadingType } from '../shared/heading-switcher/types';
import { NhsHeadingSize } from './types';

const NHS_HEADING_SIZES: Record<NhsHeadingSize, NhsHeadingType> = {
  xl: 1,
  l: 2,
  m: 3,
  s: 4,
  xs: 5
};
export { NHS_HEADING_SIZES };
